# smart-candidate-screener

> AI-powered candidate screening — scores resumes against JDs using LLMs and semantic similarity.

## Stack
Python 3.11 · OpenAI · sentence-transformers · Streamlit · pandas

## Quick Start

```bash
pip install -r requirements.txt
cp .env.example .env
streamlit run app.py
```
